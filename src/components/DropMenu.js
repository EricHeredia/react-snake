import React from 'react'

const DropMenu = (props) => {
  /* 
     Props will trigger a chain reaction of renders, slowly changing
     styling to create a dropdown animation using react hooks
  */

  const animateDown = () => {
    // Down animation here
  }

  const animateUp = () => {
    // Up animation here
  }

  return (
    <div>
      <p>DropMenu</p>
    </div>
  )
}

export default DropMenu